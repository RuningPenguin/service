import { Injectable } from '@nestjs/common';
import { <%= classify(name) %>Entity } from './<%= name %>.entity';
import { DatabaseRepository } from '@app/common';
import { DataSource } from 'typeorm';

@Injectable()
export class <%= classify(name) %>Repository extends DatabaseRepository<<%= classify(name) %>Entity> {
  constructor(dataSource: DataSource) {
    super(dataSource, <%= classify(name) %>Entity);
  }
}
